import CreateNoteModalClient from "./CreateNoteModal.client";

export default function CreateNoteModalPage() {
  // перехваченная модалка — просто рендерим клиентский компонент
  return <CreateNoteModalClient />;
}
